package com.uquiz.android.ui.designsystem.tokens

import androidx.compose.ui.unit.dp

/** Base corner radius token used consistently by shared UI primitives. */
val URadius = 8.dp

/** Backward-compatible shared radius token used by feature components. */
val AppRadius = URadius
